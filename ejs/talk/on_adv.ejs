<%
var user = 0;
var messages = [];

for (var k in this.messages)
{
  var v = this.messages[v];

  if (v.author.uid !== user && user > 0)
  {
    var obj =
    {
      author: this.users[k],
      messages: messages,
    };
%>
  <%= this.DeferRender('talk/comment', {this.users[k]})
<%
  }
}
%>